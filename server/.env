# Backend environment for Flask server
# Allowed origins for browser CORS requests
# http://localhost:5173,http://127.0.0.1:5173
ALLOWED_ORIGINS=https://sports-celebrity-classifier.vercel.app

# Flask host and port used when running server.py directly
HOST=0.0.0.0
PORT=5000
